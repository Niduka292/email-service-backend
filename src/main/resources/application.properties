# Server Configuration
spring.application.name=email-service

# Server Port
server.port=${PORT:8080}

# Database configuration (will be overridden by system properties of main)
#spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/email_system_db}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000

# Logging
logging.level.root=INFO
logging.level.com.emailapp=DEBUG

# JWT Configuration
jwt.secret=${JWT_SECRET:607b4d47b05034bfbfb3cd7cba65a73f0bc27da2fd1630c49f71eef89539923f}
jwt.expiration=${JWT_EXPIRATION:86400000}
